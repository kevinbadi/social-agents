# User artifacts — never committed (brand pack, profile map, configs, knowledge)
midas/

# Generated at onboarding for the user's agent sessions — per-workspace, not repo docs
/CLAUDE.md

# CLI-compat workspace (skill shims for scheduled runs)
creatoros/

# Automation deploy scaffolds may embed workspace copies
deploy/

node_modules/
dist/
*.log

# The agent's structured activity log (per-user data, read by the dashboard)
logs/
.env
.DS_Store
